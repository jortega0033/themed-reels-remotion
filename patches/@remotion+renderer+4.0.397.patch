diff --git a/node_modules/@remotion/renderer/dist/assets/download-file.js b/node_modules/@remotion/renderer/dist/assets/download-file.js
index 800f430..a58d5f2 100644
--- a/node_modules/@remotion/renderer/dist/assets/download-file.js
+++ b/node_modules/@remotion/renderer/dist/assets/download-file.js
@@ -33,8 +33,8 @@ const downloadFileWithoutRetries = ({ onProgress, url, to: toFn }) => {
                 if (resolved) {
                     return;
                 }
-                rejectAndFlag(new Error(`Tried to download file ${url}, but the server sent no data for 20 seconds`));
-            }, 20000);
+                rejectAndFlag(new Error(`Tried to download file ${url}, but the server sent no data for 120 seconds`));
+            }, 120000);
         };
         refreshTimeout();
         let finishEventSent = false;
diff --git a/node_modules/@remotion/renderer/dist/esm/index.mjs b/node_modules/@remotion/renderer/dist/esm/index.mjs
index 46e7366..1ca0e30 100644
--- a/node_modules/@remotion/renderer/dist/esm/index.mjs
+++ b/node_modules/@remotion/renderer/dist/esm/index.mjs
@@ -570,8 +570,8 @@ var downloadFileWithoutRetries = ({ onProgress, url, to: toFn }) => {
         if (resolved) {
           return;
         }
-        rejectAndFlag(new Error(`Tried to download file ${url}, but the server sent no data for 20 seconds`));
-      }, 20000);
+        rejectAndFlag(new Error(`Tried to download file ${url}, but the server sent no data for 120 seconds`));
+      }, 120000);
     };
     refreshTimeout();
     let finishEventSent = false;
@@ -5278,7 +5278,7 @@ var internalOpenBrowser = async ({
     logLevel,
     indent,
     userDataDir,
-    timeout: 25000,
+    timeout: 120000,
     args: [
       "about:blank",
       "--allow-pre-commit-input",
diff --git a/node_modules/@remotion/renderer/dist/open-browser.js b/node_modules/@remotion/renderer/dist/open-browser.js
index fb05a59..42c61d5 100644
--- a/node_modules/@remotion/renderer/dist/open-browser.js
+++ b/node_modules/@remotion/renderer/dist/open-browser.js
@@ -82,7 +82,7 @@ const internalOpenBrowser = async ({ browser, browserExecutable, chromiumOptions
         logLevel,
         indent,
         userDataDir,
-        timeout: 25000,
+        timeout: 120000,
         args: [
             'about:blank',
             '--allow-pre-commit-input',
